import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["res.cloudinary.com", "cdn.discordapp.com", "images.unsplash.com", "images.pexels.com"],
  },
};

export default nextConfig;
